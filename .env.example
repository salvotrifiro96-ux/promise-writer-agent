# Copy to .env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME

# Optional: protect the Streamlit app with a shared password.
# Leave empty to disable the password gate.
APP_PASSWORD=
